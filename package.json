{
  "name": "codedocent",
  "version": "0.1.0",
  "description": "CodeDocent — audience-first documentation generated from code, curated by an admin, served to non-code teams",
  "type": "module",
  "bin": { "codedocent": "./bin/codedocent.js" },
  "scripts": {
    "cli": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}

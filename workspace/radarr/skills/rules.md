# Rules (skill file — learned do/don'ts and exclusions)

## Do / Don't
<!-- Learned from admin corrections; none yet. -->

## Exclusions — never document
<!-- Paths or features that must not appear in docs (internal, deprecated,
     security-sensitive). None defined yet. Example format:
- path: src/NzbDrone.Core/Authentication/**   # security-sensitive
- feature: legacy v2 API                       # deprecated
-->
